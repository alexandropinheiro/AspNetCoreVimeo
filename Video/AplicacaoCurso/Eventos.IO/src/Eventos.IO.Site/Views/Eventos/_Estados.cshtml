@using Eventos.IO.Application.ViewModels;

@model EnderecoViewModel
@{
    var estados = (IEnumerable<SelectListItem>)new EnderecoViewModel().Estados();
}
<select asp-for="Estado" asp-items="@estados" class="form-control"></select> 